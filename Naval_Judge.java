public class Naval_Judge {
    public static void Judge() {
        // setの確認
        if (GUI_DB.flagStart) {
            if (GUI_DB.isFirst) {
                // 先攻
            } else {
                // 後攻
            }
        }
    }
}
